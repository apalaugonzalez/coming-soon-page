# SMTP Configuration for Contact Form
# Copy this file to .env.local and fill in your actual SMTP credentials

# SMTP Server Host (e.g., smtp.gmail.com, smtp.example.com)
SMTP_HOST=

# SMTP Server Port (commonly 587 for TLS, 465 for SSL, 25 for unencrypted)
SMTP_PORT=

# SMTP Username (usually your email address)
SMTP_USER=

# SMTP Password (use an app-specific password if using Gmail)
SMTP_PASSWORD=

# Email address to send from (should match SMTP_USER or be authorized)
SMTP_FROM_EMAIL=

# Email address to receive contact form submissions (defaults to info@immersegeek.com if not set)
CONTACT_RECIPIENT_EMAIL=info@immersegeek.com
